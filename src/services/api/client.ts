/**
 * Re-export axiosInstance as apiClient.
 * Backwards-compatible alias used in some test files.
 */
export { axiosInstance as apiClient } from './axios.instance';
export { axiosInstance as default } from './axios.instance';
